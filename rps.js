function computerPlay() {
    const computerNum = Math.floor(Math.random() * 3);
    let computerSelection = '';

    if (computerNum === 0) {
        return computerSelection = 'Rock';
    }
    if else (computerNum === 1) {
        return computerSelection = 'Paper';
    }
    else {
        return computerSelection = 'Sciccors';
    }
}